This is the pipe-write-a-block package meant as a guide to building
out-of-tree packages. To use the pipe blocks, the Python namespaces
is in 'pipe', which is imported as:

    import gnuradio.pipe as pipe

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(pipe)
